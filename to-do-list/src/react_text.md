*  virtual DOM: copy of Real DOM
*  Installation DOM:

*  NPM: NODE PACKAGE MANAGER
*  NPM is a platform where globally all devlopers create the application n web development feartres
*  NPX : in core level we use Npx for initialization
*  ./  : in that folder
*  ../:   in previous folder
*  run as administrator:   in linux n mac we use SUDO
commanad
*  npx create-react-app@latest
project-name: first-project
<!-- default next.jsc: yes -->
npx create-react-app first-react-app
u go on folder using cd then npm start

*  NPM START:  on local computer projects are run like go live,, DEVELOPMENT PHASE
*  npm run build:  react files are converted in static files like html, css, js ,,, DEPLOY
MEANS PRODUCTION

*  JSON:
key value pair information , in dependencies all the packages of react are install

*  PACKAGE-LOCKJ-SON: nedddedfiles are kept
*  GITIGNORE:  important files are authnticate by gitignore which is important that will not add in git


*  react is frontend js library
it faces the re-rendering means refresh or reload issue

Vite: React bundler

npm craete vite@latest
project name
react
js
no
ys

jsx:  acreatectually this is react code but it converts the html into react so in backend no need to createElement 

Component:




